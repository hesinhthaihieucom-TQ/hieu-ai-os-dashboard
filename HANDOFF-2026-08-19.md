# Bàn giao phiên làm việc — Xây Nhân Hiệu (tối 19-20/8/2026)

Dán nguyên văn file này vào tin nhắn đầu tiên của phiên Claude Code mới để tiếp tục ngay, không cần giải thích lại từ đầu.

## Bối cảnh
- Dự án: "Xây Nhân Hiệu" (SaaS xây thương hiệu cá nhân), repo `hieu-dashboard`, thư mục app chính `nhan-hieu/`.
- Live tại: `hesinhthaihieu.com/webxaynhanhieu/` (qua Cloudflare Worker proxy) và `hieu-ai-os-dashboard.vercel.app/nhan-hieu/` (Vercel gốc).
- Backend: Supabase (Postgres + Auth + RLS) + Vercel serverless functions (`api/*.js`) gọi Anthropic Claude Sonnet 5.
- Chủ dự án: Quỳnh — không rành kỹ thuật, nhắn tin ngắn/nhanh, hay kèm ảnh chụp màn hình, đang trong đợt ra mắt gấp (Zoom live tối 19/8/2026, đang bán gói ưu đãi 6/12 tháng).
- Quy trình đã thiết lập: sửa xong luôn `node --check` file rồi `git add/commit/push` thẳng lên `main`, không cần hỏi lại mỗi lần (trừ thao tác phá huỷ dữ liệu).

## ⚠️ Việc CẦN LÀM NGAY (chưa xong)
1. **Quỳnh phải tự chạy lại `supabase/schema_full.sql`** trong Supabase SQL Editor — bắt buộc để các tính năng tối nay có hiệu lực thật (bảng `module_drafts`, hàm `consume_ai_quota`/`refund_ai_quota` có thêm tham số `p_weight`). Nếu chưa chạy, mọi thứ vẫn chạy an toàn (không chặn oan ai) nhưng CHƯA có tác dụng thật.
2. **2 khách đã chuyển khoản TRƯỚC KHI fix lỗi SEVQR** (xem mục dưới) chưa được tự động kích hoạt — cần vào **Quản trị → tìm bằng mã tham chiếu → bấm "Gia hạn" + "💰 Đánh dấu đã trả phí"** để kích hoạt tay.
3. Nạp thêm tiền vào Anthropic Console (đã đề xuất 2-3 triệu đồng buffer) — Quỳnh tự làm, không phải việc code.
4. Xác nhận webhook SePay đã chạy đúng với giao dịch THẬT tiếp theo (sau fix SEVQR) — xem bảng `sepay_transactions` hoặc mục "Giao dịch SePay gần đây" ở Quản trị.

## Sự cố lớn đã xử lý tối nay (theo thứ tự)
1. **Form đăng ký bị xoá trắng** khi bấm chọn "Đã học/Chưa" → mọi input đều bị `renderAuthScreen()` render lại trắng tinh (không có `value=`) → khiến `email` rỗng khi submit → Supabase báo nhầm "Anonymous sign-ins are disabled". **Đã fix**: thêm state `authFields` giữ giá trị qua mọi lần render lại (`nhan-hieu/js/app-shell.js`).
2. **Chuyển đổi hệ thống lượt AI từ đồng giá sang trọng số thực tế** — `api/_lib/trial-quota.js` có `AI_WEIGHTS` (1-6 tuỳ hành động), phải khớp với `GATED_API_WEIGHTS` ở `nhan-hieu/js/app-shell.js` (2 nơi định nghĩa riêng, PHẢI sửa cả 2 khi đổi trọng số). Nhãn "(tốn N lượt AI)" đã cập nhật khắp các module.
3. **Nâng trần lượt trả phí 150→200/tháng** (`PAID_MONTHLY_AI_LIMIT` ở cả `api/_lib/trial-quota.js` và `nhan-hieu/js/app-shell.js`) — quyết định tạm thời, Quỳnh muốn theo dõi thực tế trước khi chốt số cuối. TRIAL_AI_LIMIT vẫn giữ 50 (trọn đời, chưa trả phí).
4. **Webhook SePay chưa từng chạy dù tiền vẫn vào tài khoản** — nguyên nhân gốc: VietinBank yêu cầu nội dung chuyển khoản phải bắt đầu bằng từ khoá **"SEVQR"** mới báo biến động số dư. QR/nội dung CK trước đó chỉ có mã tham chiếu, thiếu tiền tố này. **Đã fix** trong `nhan-hieu/js/app-shell.js` (`paymentCardHtml`/`topupCardHtml`) — nội dung CK giờ là `SEVQR <mã>`.
5. **Thêm nút "💰 Đánh dấu đã trả phí"** ở Quản trị (`nhan-hieu/js/quan-tri.js`) — vì nút "Gia hạn" chỉ cộng ngày dùng, KHÔNG tự bật `has_paid`, cần cả 2 khi kích hoạt tay.
6. **Thêm Trang chủ** (`nhan-hieu/js/home.js`) — chào mừng + checklist 4 bước (Định Vị/Sửa Kênh/Viết bài đầu/Lịch đăng), là màn mặc định khi đăng nhập, bấm logo sidebar để quay lại.
7. **Rà soát toàn bộ "giữ trạng thái khi chuyển trang"** (`module_drafts` table + `loadModuleDraft`/`saveModuleDraft`/`clearModuleDraft` trong `nhan-hieu/js/util.js`) — đã phủ đủ: Viết Content, Tái Chế Viral, Đẩy Bài, Lịch Đăng (bảng riêng `weekly_ai_drafts`), Chấm Điểm Content/Hook (dùng lại lịch sử), **Định Vị** (18 câu hỏi đang trả lời dở), **Sửa Kênh** (ảnh + câu trả lời đang làm dở), **Kho Hook** (tab Tạo Hook), **Tạo Ảnh Thương Hiệu** (ảnh + tuỳ chỉnh). Tạo Ảnh không gọi AI nên không tính lượt.
8. **Fix tải ảnh PNG không hoạt động trên desktop** (`nhan-hieu/js/tao-anh.js`) — nguyên nhân: `canvas.toBlob` bất đồng bộ rồi mới `a.click()` trong callback khiến trình duyệt desktop coi thao tác bấm đã hết hạn, im lặng chặn tải. Fix: dùng `toDataURL` đồng bộ cho nhánh desktop (không có Web Share API), giữ nguyên nhánh mobile.
9. **Bug hiển thị sai "Đã lưu ✓"** ở Kho Hook — `genThumbSavedIdx` không được reset khi sinh lô hook mới, khiến tiêu đề MỚI hiện nhầm là đã lưu (trùng vị trí index với lô cũ). Đã fix.
10. Nhiều sửa nhỏ khác: badge "🔥 Đóng góp Kho Viral" làm nổi bật lại (giữ validation ≥200k), nút Reset cho Viết Content/Tái Chế Viral/Đẩy Bài, thông báo trấn an khi chờ AI viết bài quá 1-2 phút (`startWaitReassurance` trong `util.js`, mới áp dụng cho Viết Content — có thể mở rộng sang Tái Chế Viral/Sửa Kênh/Định Vị nếu cần), Quản trị hiện thêm cột "Đã dùng lượt AI", tìm kiếm Quản trị theo mã tham chiếu.

## Kiến trúc cần nhớ
- SPA thuần HTML/CSS/JS, hash routing, mỗi module là 1 file `nhan-hieu/js/<ten>.js` dạng IIFE đăng ký `window.Modules['key'] = {title, render(container, ctx)}`.
- **`app-shell.js` KHÔNG bọc IIFE** — biến/hàm top-level của nó chia sẻ global scope với các script khác load cùng trang (cẩn thận trùng tên).
- `ctx` truyền vào mỗi module gồm `{ supabase, user, profile }`.
- Style code: comment tiếng Việt giải thích LÝ DO (không phải mô tả code làm gì), ưu tiên sửa tối giản đúng yêu cầu, không lan sang refactor ngoài phạm vi.
- Luôn `node --check <file>` trước khi commit. Không bao giờ `git push --force`. Test nhanh bằng cách serve `nhan-hieu/` qua `python3 -m http.server` + mock `window.supabaseClient` khi cần xem trước không cần đăng nhập thật.

## Việc còn treo, chưa làm
- Chưa mở rộng `startWaitReassurance` (thông báo chờ lâu) sang Tái Chế Viral/Sửa Kênh/Định Vị — Quỳnh nói "nếu hiệu quả thì làm thêm", chưa xác nhận.
- Chưa xác nhận với Quỳnh việc nâng cấp mức lượt 200/tháng có ổn theo dữ liệu thực tế (cần theo dõi qua Quản trị vài ngày).
- Chưa test lại thực tế nút tải ảnh PNG trên máy tính sau fix (vừa code xong, chưa có xác nhận từ Quỳnh).
